# IDA

IDA (Identity-Deterministic Annotation) provides per-tile annotations whose
SHA-256 hashes are verified end-to-end to enforce the `compute = memory = identity` invariant.

See `spec/b4iu_tile_v0.1.md` for the `ida_payload` / `ida_hash` field specification.
